<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Lời chúc 20/10 💖</title>
<style>
  body {
    background-color: #fff0f5;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    font-family: 'Segoe UI', sans-serif;
    text-align: center;
    margin: 0;
    padding: 20px;
    box-sizing: border-box;
  }

  .container {
    background-color: rgba(255, 255, 255, 0.9);
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 10px 30px rgba(214, 51, 132, 0.2);
    z-index: 2;
    max-width: 90%;
    position: relative;
    overflow: hidden;
  }

  h1 {
    color: #d63384;
    font-size: 1.8em;
    margin-bottom: 20px;
    line-height: 1.4;
  }

  .hint {
    color: #888;
    font-size: 1em;
    margin-top: 10px;
    animation: blink 1.5s infinite alternate;
  }

  @keyframes blink {
    from { opacity: 0.6; }
    to { opacity: 1; }
  }

  /* Tim bay */
  .heart {
    position: absolute;
    color: #ff4d88;
    font-size: 24px;
    animation: float 5s infinite ease-in;
    pointer-events: none;
    z-index: 1;
  }

  @keyframes float {
    0% { transform: translateY(0) scale(1); opacity: 1; }
    100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
  }

  /* Ảnh bay quanh */
  .flying-img {
    position: absolute;
    width: 120px; /* Giảm kích thước để không che khuất nội dung */
    border-radius: 12px;
    pointer-events: none;
    z-index: 1;
    box-shadow: 0 0 15px rgba(255, 105, 180, 0.6);
    opacity: 0.7; /* Giảm độ đậm để không làm phân tâm */
  }

  /* Nút điều khiển nhạc */
  .music-control {
    background-color: #d63384;
    color: white;
    border: none;
    border-radius: 50px;
    padding: 10px 20px;
    margin-top: 20px;
    font-size: 1em;
    cursor: pointer;
    transition: all 0.3s ease;
    z-index: 3;
  }

  .music-control:hover {
    background-color: #b82a6f;
    transform: scale(1.05);
  }

  /* Hiệu ứng hoa */
  .flower {
    position: absolute;
    font-size: 20px;
    animation: float 6s infinite ease-in-out;
    pointer-events: none;
    z-index: 1;
  }

  /* Hiệu ứng pháo hoa */
  .firework {
    position: absolute;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    pointer-events: none;
    z-index: 1;
  }

  /* Hiệu ứng chữ */
  .message {
    font-size: 1.2em;
    color: #d63384;
    margin-top: 20px;
    opacity: 0;
    transition: opacity 1s ease;
    font-style: italic;
  }

  .message.show {
    opacity: 1;
  }
</style>
</head>
<body>

  <div class="container">
    <h1>
      Con chúc mẹ 20/10 thật nhiều sức khỏe,<br>luôn vui vẻ và hạnh phúc.
    </h1>
    
    <p class="hint">👉 Chạm vào màn hình để có bất ngờ nhé!</p>
    
    <button class="music-control" id="musicBtn">🎵 Bật nhạc</button>
    
    <div class="message" id="specialMessage">
      Mẹ là người phụ nữ tuyệt vời nhất trong cuộc đời con! 💕
    </div>
  </div>

  <audio id="music" loop>
    <source src="https://assets.mixkit.co/music/preview/mixkit-happy-moments-87.mp3" type="audio/mpeg">
    Trình duyệt của bạn không hỗ trợ phát nhạc.
  </audio>

  <script>
    // Tim bay ngẫu nhiên
    function createHeart() {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.textContent = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = Math.random() * 2 + 3 + "s";
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 5000);
    }
    setInterval(createHeart, 300);

    // Hoa bay ngẫu nhiên
    function createFlower() {
      const flower = document.createElement("div");
      flower.classList.add("flower");
      flower.textContent = "🌸";
      flower.style.left = Math.random() * 100 + "vw";
      flower.style.animationDuration = Math.random() * 3 + 4 + "s";
      document.body.appendChild(flower);
      setTimeout(() => flower.remove(), 7000);
    }
    setInterval(createFlower, 500);

    // Điều khiển nhạc
    const music = document.getElementById("music");
    const musicBtn = document.getElementById("musicBtn");
    let isPlaying = false;
    
    musicBtn.addEventListener("click", () => {
      if (isPlaying) {
        music.pause();
        musicBtn.textContent = "🎵 Bật nhạc";
      } else {
        music.play().catch(e => {
          console.log("Lỗi phát nhạc:", e);
          alert("Nhạc không thể phát tự động. Vui lòng nhấn nút 'Bật nhạc' để nghe.");
        });
        musicBtn.textContent = "⏸️ Tạm dừng";
      }
      isPlaying = !isPlaying;
    });

    // --- Ảnh bay quanh màn hình ---
    const imgSrc = "https://via.placeholder.com/200x150/ffb6c1/ffffff?text=Ảnh+Mẹ"; // ảnh placeholder
    const numImages = 5; // Giảm số lượng ảnh để không che khuất nội dung

    function createFlyingImage() {
      const img = document.createElement("img");
      img.src = imgSrc;
      img.classList.add("flying-img");
      img.style.left = Math.random() * 100 + "vw";
      img.style.top = Math.random() * 100 + "vh";
      document.body.appendChild(img);

      // Chuyển động ngẫu nhiên
      const xMove = Math.random() * 100 - 50;
      const yMove = Math.random() * 100 - 50;

      img.animate(
        [
          { transform: "translate(0, 0) rotate(0deg)" },
          { transform: `translate(${xMove}px, ${yMove}px) rotate(${Math.random() * 360}deg)` }
        ],
        {
          duration: 3000 + Math.random() * 2000,
          direction: "alternate",
          iterations: Infinity,
          easing: "ease-in-out"
        }
      );
    }

    for (let i = 0; i < numImages; i++) {
      createFlyingImage();
    }

    // Hiệu ứng khi click vào màn hình
    document.addEventListener("click", (e) => {
      // Tạo pháo hoa tại vị trí click
      createFirework(e.clientX, e.clientY);
      
      // Hiển thị thông điệp đặc biệt
      const message = document.getElementById("specialMessage");
      message.classList.add("show");
      
      // Ẩn thông điệp sau 5 giây
      setTimeout(() => {
        message.classList.remove("show");
      }, 5000);
    });

    // Tạo hiệu ứng pháo hoa
    function createFirework(x, y) {
      const colors = ['#ff4d88', '#d63384', '#ff80ab', '#ff4081', '#f50057'];
      
      for (let i = 0; i < 30; i++) {
        const particle = document.createElement("div");
        particle.classList.add("firework");
        particle.style.left = x + "px";
        particle.style.top = y + "px";
        particle.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        document.body.appendChild(particle);
        
        const angle = Math.random() * Math.PI * 2;
        const speed = 2 + Math.random() * 3;
        const vx = Math.cos(angle) * speed;
        const vy = Math.sin(angle) * speed;
        
        particle.animate(
          [
            { transform: 'translate(0, 0)', opacity: 1 },
            { transform: `translate(${vx * 100}px, ${vy * 100}px)`, opacity: 0 }
          ],
          {
            duration: 1000 + Math.random() * 500,
            easing: 'ease-out'
          }
        );
        
        setTimeout(() => particle.remove(), 1500);
      }
    }
  </script>

</body>
</html>
